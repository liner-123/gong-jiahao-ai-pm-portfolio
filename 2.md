# 修改讨论记录

## 1. 首页学历与按钮调整

- 同济大学学历卡片中间增加 `985` 标签。
- 江南大学学历卡片中间增加 `211` 标签。
- 去掉“下载简历”和“查看案例”两个按钮。

## 2. 联系区域简历下载移除

- 去掉页面底部联系区域里的“下载完整简历”按钮。
- 移除对应的简历下载功能，避免简历文件被访客直接下载。

## 3. 个人 AI 实践截图与证书入口调整

- `C:\Users\18474\Desktop\个人ai截图汇总` 里每个文件夹对应一个个人 AI 实践的截图。
- “论文与科研工作流 Skill 体系”这个实践项里去掉“查看截图”按钮。
- 大模型证书截图路径为：`C:\Users\18474\Desktop\比赛证书.png`。
- 在“成果与证书”区域给“大模型”增加“查看证书”入口，指向上面的证书截图。

## 4. ReactBits MCP 视觉组件替换

- 后续修改需要调用 `reactbits` MCP 服务里的组件。
- 将以下大标题改为使用 `gradient-text` 组件展示：
  - AI 产品落地能力地图
  - 企业与比赛产品案例
  - AI 产品稳定性与评测方法
  - 个人 AI 实践
  - 成果与证书
- 将文案“好的 AI 产品不是把模型接进流程，而是把业务目标、可控边界和评测闭环一起放进交付标准。”改为使用 `blur-text` 组件展示。
- 首页，也就是包含个人信息的第一屏背景，改为使用 `liquid-ether` 组件，并保留动态效果。
- 后续每一屏/每个页面的背景改为使用 `threads` 组件。
- 上述组件都来自 `reactbits` MCP。
